// @ts-check

/** @type {import("./types").PrettierConfig} */
const config = {
    arrowParens: "avoid",
    bracketSpacing: false,
    printWidth: 200,
    semi: true,
    tabWidth: 4,
    trailingComma: "es5",
};

module.exports = config;
