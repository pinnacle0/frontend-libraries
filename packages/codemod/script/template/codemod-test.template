import {createTransform} from "./createTransform";

const transform = createTransform("// {{KEBAB_CASE_CODEMOD_NAME}}");

// Put your tests here
describe("Testing // {{KEBAB_CASE_CODEMOD_NAME}} codemod", () => {
    test.todo("use createTest to write the test");
});

