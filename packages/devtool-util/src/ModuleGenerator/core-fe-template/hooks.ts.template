import {useSelector} from "core-fe";
import type {RootState} from "type/state";

export function use{1}State<T>(fn: (state: RootState["app"]["{2}"]) => T): T {
    return useSelector((state: RootState) => fn(state.app.{2}));
}
