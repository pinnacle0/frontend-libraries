// @ts-check

/** @type {import("prettier").Options} */
const config = {
    arrowParens: "avoid",
    bracketSpacing: false,
    printWidth: 200,
    semi: true,
    tabWidth: 4,
    trailingComma: "es5",
};

export default config;
